{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php-fpm -F & nginx -c /app/nginx.conf -g 'daemon off;'",
        "healthcheckPath": "/info.php",
        "healthcheckTimeout": 300
    }
}
