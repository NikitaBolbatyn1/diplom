{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php-fpm -F -y /etc/php-fpm.conf & nginx -c /app/nginx.conf -g 'daemon off;'",
        "healthcheckPath": "/info.php",
        "healthcheckTimeout": 300
    }
}
